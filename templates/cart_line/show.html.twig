{% extends 'base.html.twig' %}

{% block title %}CartLine{% endblock %}

{% block body %}
<div class="container">
<div class="row">
<div class="col-md-6">

    <h1>CartLine</h1>

    <table class="table table-condensed">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cart_line.id }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ cart_line.quantity }}</td>
            </tr>
            <tr>
                <th>Variant</th>
                <td>{{ cart_line.variant }}</td>
            </tr>
            <tr>
                <th>Cart</th>
                <td>{{ cart_line.cart }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('cart_line_index') }}">back to list</a>

    <a href="{{ path('cart_line_edit', {'id': cart_line.id}) }}">edit</a>

    {{ include('cart_line/_delete_form.html.twig') }}
    </div></div></div>
{% endblock %}
