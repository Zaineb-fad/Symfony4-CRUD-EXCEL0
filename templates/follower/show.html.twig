{% extends 'base.html.twig' %}

{% block title %}Follower{% endblock %}

{% block body %}
<div class="container">
<div class="row">
<div class="col-md-6">

    <h1>Follower</h1>

    <table class="table table-condensed">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ follower.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ follower.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('follower_index') }}">back to list</a>

    <a href="{{ path('follower_edit', {'id': follower.id}) }}">edit</a>

    {{ include('follower/_delete_form.html.twig') }}
    </div></div></div>
{% endblock %}
