{% extends 'base.html.twig' %}

{% block title %}Transportation{% endblock %}

{% block body %}
<div class="container">
<div class="row">
<div class="col-md-6">

    <h1>Transportation</h1>

    <table class="table table-condensed">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ transportation.id }}</td>
            </tr>
            <tr>
                <th>Color</th>
                <td>{{ transportation.color }}</td>
            </tr>
            <tr>
                <th>LicencePlate</th>
                <td>{{ transportation.licencePlate }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('transportation_index') }}">back to list</a>

    <a href="{{ path('transportation_edit', {'id': transportation.id}) }}">edit</a>

    {{ include('transportation/_delete_form.html.twig') }}
    </div></div></div>
{% endblock %}
