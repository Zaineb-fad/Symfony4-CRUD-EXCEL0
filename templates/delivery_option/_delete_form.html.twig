<form method="post" action="{{ path('delivery_option_delete', {'id': delivery_option.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ delivery_option.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
