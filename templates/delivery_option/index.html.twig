{% extends 'base.html.twig' %}

{% block title %}DeliveryOption index{% endblock %}

{% block body %}
<div class="container">
<div class="row">
<div class="col-md-6">

    <h1>DeliveryOption index</h1>

    <table class="table table-condensed">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for delivery_option in delivery_options %}
            <tr>
                <td>{{ delivery_option.id }}</td>
                <td>{{ delivery_option.name }}</td>
                <td>
                    <a href="{{ path('delivery_option_show', {'id': delivery_option.id}) }}"class="btn btn-success">show</a>
                    <a href="{{ path('delivery_option_edit', {'id': delivery_option.id}) }}"class="btn btn-info">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('delivery_option_new') }}"class="btn btn-lg">Create new</a></div></div></div>
{% endblock %}
