{% extends 'base.html.twig' %}

{% block title %}DeliveryOption{% endblock %}

{% block body %}
<div class="container">
<div class="row">
<div class="col-md-6">

    <h1>DeliveryOption</h1>

    <table class="table table-condensed">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ delivery_option.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ delivery_option.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('delivery_option_index') }}">back to list</a>

    <a href="{{ path('delivery_option_edit', {'id': delivery_option.id}) }}">edit</a>

    {{ include('delivery_option/_delete_form.html.twig') }}
    </div></div></div>
{% endblock %}
