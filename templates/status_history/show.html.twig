{% extends 'base.html.twig' %}

{% block title %}StatusHistory{% endblock %}

{% block body %}
<div class="container">
<div class="row">
<div class="col-md-6">

    <h1>StatusHistory</h1>

    <table class="table table-condensed">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ status_history.id }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ status_history.status }}</td>
            </tr>
            <tr>
                <th>StatusDate</th>
                <td>{{ status_history.statusDate ? status_history.statusDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>StatusOrder</th>
                <td>{{ status_history.statusOrder }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('status_history_index') }}">back to list</a>

    <a href="{{ path('status_history_edit', {'id': status_history.id}) }}">edit</a>

    {{ include('status_history/_delete_form.html.twig') }}
    </div></div<</div>
{% endblock %}
